version: '3.8'
services:
  mongo:
    container_name: mongo-container
    image: mongo:6
    volumes:
      - ./data:/data/db
    ports:
      - '27017:27017'
  api:
    container_name: api-container
    build: .
    image: clean-ts-api
    command: npm start
    volumes:
      - ./dist:/usr/src/clean-ts-api/dist
    ports:
      - '3030:3030'
    links:
      - mongo
